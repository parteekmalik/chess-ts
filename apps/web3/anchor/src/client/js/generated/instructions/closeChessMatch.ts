/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { WEB3_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_CHESS_MATCH_DISCRIMINATOR = new Uint8Array([
  114, 13, 159, 131, 41, 71, 181, 151,
]);

export function getCloseChessMatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_CHESS_MATCH_DISCRIMINATOR
  );
}

export type CloseChessMatchInstruction<
  TProgram extends string = typeof WEB3_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountChessMatch extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountChessMatch extends string
        ? WritableAccount<TAccountChessMatch>
        : TAccountChessMatch,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseChessMatchInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseChessMatchInstructionDataArgs = {};

export function getCloseChessMatchInstructionDataEncoder(): FixedSizeEncoder<CloseChessMatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_CHESS_MATCH_DISCRIMINATOR })
  );
}

export function getCloseChessMatchInstructionDataDecoder(): FixedSizeDecoder<CloseChessMatchInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseChessMatchInstructionDataCodec(): FixedSizeCodec<
  CloseChessMatchInstructionDataArgs,
  CloseChessMatchInstructionData
> {
  return combineCodec(
    getCloseChessMatchInstructionDataEncoder(),
    getCloseChessMatchInstructionDataDecoder()
  );
}

export type CloseChessMatchInput<
  TAccountPayer extends string = string,
  TAccountChessMatch extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  chessMatch: Address<TAccountChessMatch>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseChessMatchInstruction<
  TAccountPayer extends string,
  TAccountChessMatch extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WEB3_PROGRAM_ADDRESS,
>(
  input: CloseChessMatchInput<
    TAccountPayer,
    TAccountChessMatch,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CloseChessMatchInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountChessMatch,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WEB3_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    chessMatch: { value: input.chessMatch ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.chessMatch),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getCloseChessMatchInstructionDataEncoder().encode({}),
  } as CloseChessMatchInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountChessMatch,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedCloseChessMatchInstruction<
  TProgram extends string = typeof WEB3_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    chessMatch: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CloseChessMatchInstructionData;
};

export function parseCloseChessMatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseChessMatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      chessMatch: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseChessMatchInstructionDataDecoder().decode(instruction.data),
  };
}
