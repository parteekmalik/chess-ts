/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { WEB3_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const MAKE_MOVE_DISCRIMINATOR = new Uint8Array([
  78, 77, 152, 203, 222, 211, 208, 233,
]);

export function getMakeMoveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MAKE_MOVE_DISCRIMINATOR);
}

export type MakeMoveInstruction<
  TProgram extends string = typeof WEB3_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountChessMatch extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountPlayerProfile extends string | AccountMeta<string> = string,
  TAccountOpponentProfile extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountChessMatch extends string
        ? WritableAccount<TAccountChessMatch>
        : TAccountChessMatch,
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountPlayerProfile extends string
        ? WritableAccount<TAccountPlayerProfile>
        : TAccountPlayerProfile,
      TAccountOpponentProfile extends string
        ? WritableAccount<TAccountOpponentProfile>
        : TAccountOpponentProfile,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MakeMoveInstructionData = {
  discriminator: ReadonlyUint8Array;
  moveFenStr: string;
};

export type MakeMoveInstructionDataArgs = { moveFenStr: string };

export function getMakeMoveInstructionDataEncoder(): Encoder<MakeMoveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['moveFenStr', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: MAKE_MOVE_DISCRIMINATOR })
  );
}

export function getMakeMoveInstructionDataDecoder(): Decoder<MakeMoveInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['moveFenStr', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getMakeMoveInstructionDataCodec(): Codec<
  MakeMoveInstructionDataArgs,
  MakeMoveInstructionData
> {
  return combineCodec(
    getMakeMoveInstructionDataEncoder(),
    getMakeMoveInstructionDataDecoder()
  );
}

export type MakeMoveAsyncInput<
  TAccountPayer extends string = string,
  TAccountChessMatch extends string = string,
  TAccountRegistry extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountOpponentProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  chessMatch: Address<TAccountChessMatch>;
  registry?: Address<TAccountRegistry>;
  playerProfile?: Address<TAccountPlayerProfile>;
  opponentProfile: Address<TAccountOpponentProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  moveFenStr: MakeMoveInstructionDataArgs['moveFenStr'];
};

export async function getMakeMoveInstructionAsync<
  TAccountPayer extends string,
  TAccountChessMatch extends string,
  TAccountRegistry extends string,
  TAccountPlayerProfile extends string,
  TAccountOpponentProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WEB3_PROGRAM_ADDRESS,
>(
  input: MakeMoveAsyncInput<
    TAccountPayer,
    TAccountChessMatch,
    TAccountRegistry,
    TAccountPlayerProfile,
    TAccountOpponentProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  MakeMoveInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountChessMatch,
    TAccountRegistry,
    TAccountPlayerProfile,
    TAccountOpponentProfile,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WEB3_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    chessMatch: { value: input.chessMatch ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    opponentProfile: { value: input.opponentProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 103, 105, 115, 116, 114, 121])
        ),
      ],
    });
  }
  if (!accounts.playerProfile.value) {
    accounts.playerProfile.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [getAddressEncoder().encode(expectAddress(accounts.payer.value))],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.chessMatch),
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.playerProfile),
      getAccountMeta(accounts.opponentProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getMakeMoveInstructionDataEncoder().encode(
      args as MakeMoveInstructionDataArgs
    ),
  } as MakeMoveInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountChessMatch,
    TAccountRegistry,
    TAccountPlayerProfile,
    TAccountOpponentProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type MakeMoveInput<
  TAccountPayer extends string = string,
  TAccountChessMatch extends string = string,
  TAccountRegistry extends string = string,
  TAccountPlayerProfile extends string = string,
  TAccountOpponentProfile extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  chessMatch: Address<TAccountChessMatch>;
  registry: Address<TAccountRegistry>;
  playerProfile: Address<TAccountPlayerProfile>;
  opponentProfile: Address<TAccountOpponentProfile>;
  systemProgram?: Address<TAccountSystemProgram>;
  moveFenStr: MakeMoveInstructionDataArgs['moveFenStr'];
};

export function getMakeMoveInstruction<
  TAccountPayer extends string,
  TAccountChessMatch extends string,
  TAccountRegistry extends string,
  TAccountPlayerProfile extends string,
  TAccountOpponentProfile extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WEB3_PROGRAM_ADDRESS,
>(
  input: MakeMoveInput<
    TAccountPayer,
    TAccountChessMatch,
    TAccountRegistry,
    TAccountPlayerProfile,
    TAccountOpponentProfile,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): MakeMoveInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountChessMatch,
  TAccountRegistry,
  TAccountPlayerProfile,
  TAccountOpponentProfile,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WEB3_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    chessMatch: { value: input.chessMatch ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    playerProfile: { value: input.playerProfile ?? null, isWritable: true },
    opponentProfile: { value: input.opponentProfile ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.chessMatch),
      getAccountMeta(accounts.registry),
      getAccountMeta(accounts.playerProfile),
      getAccountMeta(accounts.opponentProfile),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getMakeMoveInstructionDataEncoder().encode(
      args as MakeMoveInstructionDataArgs
    ),
  } as MakeMoveInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountChessMatch,
    TAccountRegistry,
    TAccountPlayerProfile,
    TAccountOpponentProfile,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedMakeMoveInstruction<
  TProgram extends string = typeof WEB3_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    chessMatch: TAccountMetas[1];
    registry: TAccountMetas[2];
    playerProfile: TAccountMetas[3];
    opponentProfile: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: MakeMoveInstructionData;
};

export function parseMakeMoveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedMakeMoveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      chessMatch: getNextAccount(),
      registry: getNextAccount(),
      playerProfile: getNextAccount(),
      opponentProfile: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMakeMoveInstructionDataDecoder().decode(instruction.data),
  };
}
